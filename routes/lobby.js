var express = require('express');
var router = express.Router({});
var app = express();


// Generating new lobby code
app.post('/', function(req, res, next){
    
});


// Join existing lobby
app.get('/', function(req, res, next){

});


module.exports = router;